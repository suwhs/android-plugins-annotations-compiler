apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile '<plugin-annotations-jcenter-uri-must-be-placed-here'>
    compile 'com.google.auto.service:auto-service:+'
}
